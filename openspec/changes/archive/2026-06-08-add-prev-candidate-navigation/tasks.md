## 1. エンジン実装

- [x] 1.1 `processConversion`（engine.ts）に `x` / `Shift+Tab` で前候補へ戻る分岐を追加する

## 2. テスト追加

- [x] 2.1 `x` キーで前候補に戻るテストを engine.test.ts に追加する
- [x] 2.2 `Shift+Tab` で前候補に戻るテストを engine.test.ts に追加する
- [x] 2.3 先頭候補（index=0）で `x` を押したとき no-op になるテストを追加する

## 3. 動作確認

- [x] 3.1 テストスイートが全件パスすることを確認する
