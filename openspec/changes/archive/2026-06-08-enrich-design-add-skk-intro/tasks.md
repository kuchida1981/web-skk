## 1. JSX 変更（App.tsx）

- [x] 1.1 `app__header-title` div でタイトルをラップし、subtitle 段落を追加する

## 2. デザイン変更（App.css）

- [x] 2.1 `.app__header` にアクセントバー（`border-top` + グラデーション）と白背景・下シャドウを追加する
- [x] 2.2 `.app__title` の文字色を indigo に変更する
- [x] 2.3 `.app__subtitle` のスタイル（フォントサイズ・色・余白）を定義する
- [x] 2.4 `.app__header-title` の縦 flex レイアウトを定義する
- [x] 2.5 `.skk-input-area` に通常状態の subtle な `box-shadow` を追加する

## 3. グローバルスタイル調整（index.css）

- [x] 3.1 `body` の背景色をわずかに indigo がかった白（`#f0f4ff` など）に調整する
